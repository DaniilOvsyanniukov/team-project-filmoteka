const searchForm = document.querySelector('.search-form');
const gallery = document.querySelector('.gallery');
const movieAbout = document.querySelector('.movie-id');

export default {
  searchForm,
  gallery,
  movieAbout,
};
